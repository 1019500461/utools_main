name: Keep Awake

on:
  schedule:
    - cron: "0 2 */4 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend and database
        run: curl -fsS --retry 3 --retry-delay 10 https://utools-main.onrender.com/health/db
